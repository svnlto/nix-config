run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck
    - gocritic
    - revive
    - misspell
    - prealloc
    - exportloopref

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
